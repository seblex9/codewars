// Given an integer or a floating-point number,
// find its opposite.

// short
function opposite(number) {
  return number * -1;
}

// shorter
function opposite(number) {
  return -number;
}

// shortest
const opposite = (number) => -number;
